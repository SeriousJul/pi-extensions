import{_ as t,o,c as s,a0 as a}from"./chunks/framework.ChlVJx0I.js";const n="/pi-extensions/assets/initial-context.BqQS8CoP.png",w=JSON.parse('{"title":"initial-context extension","description":"","frontmatter":{},"headers":[],"relativePath":"extensions/initial-context.md","filePath":"extensions/initial-context.md","lastUpdated":1789777524000}'),i={name:"extensions/initial-context.md"};function r(d,e,c,l,h,p){return o(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="initial-context-extension" tabindex="-1">initial-context extension <a class="header-anchor" href="#initial-context-extension" aria-label="Permalink to &quot;initial-context extension&quot;">​</a></h1><p>The token breakdown of the running agent&#39;s initial context: the system prompt and the tool definitions, which are resent to the model on every LLM call. One row per section and per tool, with token counts, percentages, and bars. Tool rows also show the tool&#39;s call count in a usage window (the last 30 days by default), so the cost of a tool can be weighed against how often it is used.</p><h2 id="screenshot" tabindex="-1">Screenshot <a class="header-anchor" href="#screenshot" aria-label="Permalink to &quot;Screenshot&quot;">​</a></h2><p>The <code>/ctx</code> view: one row per section and per tool - the base-prompt boilerplate, the available-tools snippet block, and one row per prompt guideline - with tokens, percentages of the context window, and the 30-day tool call counts.</p><p><img src="'+n+'" alt="initial context TUI"></p><h2 id="ctx-tui" tabindex="-1">/ctx (TUI) <a class="header-anchor" href="#ctx-tui" aria-label="Permalink to &quot;/ctx (TUI)&quot;">​</a></h2><p>Opens the breakdown. There is one row per part: the base-prompt region (ADR 0021), the append text, project instruction files, skills, the cwd line, a prompt injection when another extension touched the prompt, and each tool. Rows are sorted by size, largest first.</p><p>The default base prompt splits into report-sized rows. The <code>base prompt</code> row holds the boilerplate: the identity line, the custom-tools line, and the pi documentation block. The <code>available tools</code> row carries all the Prompt snippets (the one-line entries tools register). Each Prompt guideline gets its own row, attributed to its source: <code>builtin</code> for pi&#39;s default bullets, <code>extension</code> for the rest. A custom prompt stays a single row, as it has always been. The split changes presentation only: the section texts still concatenate to the exact base prompt, so the prompt injection detection and the token totals are unchanged.</p><p>Once the session has at least one provider request, a guideline whose text largely restates a sent tool description shows a <code>duplicates tool description</code> note, in the text table and the TUI (ADR 0022). The tool description is already resent on every call as part of the tool entry, so such a guideline pays cost without adding information. The check is deliberately conservative - it catches verbatim and prefixed-verbatim restatements, not paraphrases - and it compares against the descriptions of the last captured provider request, the text actually sent. Before the first provider request the rows show with no note, and the available-tools snippets are never flagged: the one-line restatement is the design of that section.</p><p>Every row shows its source (builtin, settings, file, skill, or extension; a tool is builtin or the extension that added it), its token count, its share of the initial context total, its share of the context window, and a bar. Tool rows additionally show their call count in the usage window. Skill rows show how often the agent loaded that skill&#39;s SKILL.md (a rough proxy: it counts the load, not the outcome). The other section rows show <code>-</code>, and TOTAL sums the tool calls.</p><p>With a usage view, a derived <strong>waste</strong> column appears (TUI and text): for a tool row with uses in the window it shows the cost a use pays (tokens per use), and for a tool row with zero uses it shows <code>never</code>, the mark that started the review of issue #72. The value is computed in the pure renderer from data the report already carries (the row&#39;s tokens and its call count); the usage scan and the window logic are untouched. Section rows and TOTAL show <code>-</code>.</p><p>The usage window is the last 30 days by default. <code>w</code> cycles 30d → 90d → all. In headless modes the command takes the window as an argument: <code>/ctx 90d</code>, <code>/ctx all</code>.</p><table tabindex="0"><thead><tr><th>Key</th><th>Action</th></tr></thead><tbody><tr><td><code>j</code> / <code>k</code></td><td>move the cursor (scroll the pane when expanded)</td></tr><tr><td><code>e</code></td><td>expand the row to the exact text, or collapse</td></tr><tr><td><code>c</code></td><td>copy the expanded row, or the whole breakdown</td></tr><tr><td><code>w</code></td><td>cycle the usage window (30d, 90d, all)</td></tr><tr><td><code>esc</code> / <code>q</code></td><td>close</td></tr></tbody></table><p>Expanding the <code>mcp</code> row lists its per-subtool split (<code>mcp</code> calls with a <code>tool</code> argument count as that subtool; <code>connect</code>, <code>describe</code>, and <code>search</code> count as plain <code>mcp</code>).</p><p>A footer status line keeps the total visible without opening the view: <code>ctx: 45.2K (11.3%)</code>. The count is compact and uses the system locale&#39;s number format.</p><p>In headless modes the command prints the same breakdown as plain text: a notify record in RPC mode, the console in print mode.</p><h2 id="how-the-numbers-are-built" tabindex="-1">How the numbers are built <a class="header-anchor" href="#how-the-numbers-are-built" aria-label="Permalink to &quot;How the numbers are built&quot;">​</a></h2><p>The prompt rows come from the same structured inputs pi uses to build the system prompt (base template, append text, project files, skills, cwd). The base-template rows (boilerplate, snippets, guidelines) are the structured pieces of the default prompt, and their texts concatenate to the exact base prompt pi sends, which is what the injection detection compares against (ADR 0021). Tool rows come from the tool entries actually sent to the provider, captured read-only from the last provider request payload. Before the first call, built-in schemas are rebuilt from pi&#39;s exported tool factories and marked <code>built-in schema</code>; tools that cannot be resolved are listed by name with <code>waiting for first call</code>.</p><p>Token counts use pi&#39;s own estimator (chars / 4), so every row shares one consistent scale. The first assistant response also records the input tokens the provider reported, shown as a reference line. It is display only: the bar and percentages stay comparable across rows.</p><p>If other extensions append to the prompt, an injection row shows the suffix; anything else that changes the prompt is flagged <code>modified by extension</code>.</p><h2 id="how-the-uses-column-is-built" tabindex="-1">How the uses column is built <a class="header-anchor" href="#how-the-uses-column-is-built" aria-label="Permalink to &quot;How the uses column is built&quot;">​</a></h2><p>The counts are derived from the session files: a tool call is a <code>toolCall</code> item inside an assistant message, and a fork-copied line counts once (ADR 0009). A skill load is a tool call whose arguments reference the skill&#39;s SKILL.md, by path; the skill name is read off the directory that holds the file. The scan runs in the background on the first <code>/ctx</code>; the dialog shows a counting state until it settles. A scan that fails settles to an error state, and the next <code>/ctx</code> re-runs it. A per-file cache keyed on mtime and size (<code>~/.pi/agent/tool-usage-cache.json</code>, override <code>PI_TOOL_USAGE_CACHE</code>) makes every later open near instant and window switches a filter on cached events; the cache is a pure function of the session files, never a source of truth (ADR 0014). The sessions root is <code>~/.pi/agent/sessions</code>, override <code>PI_SESSIONS_DIR</code>.</p><p>This extension registers no tools, no prompt text, and no prompt notes of its own, so its own overhead is zero.</p><h2 id="files" tabindex="-1">Files <a class="header-anchor" href="#files" aria-label="Permalink to &quot;Files&quot;">​</a></h2><ul><li><code>context.ts</code> - pure core: row construction, payload parsing, injection detection, token estimation, plain-text rendering</li><li><code>tool-usage.ts</code> - the tool usage scan, cache, and windows</li><li><code>tui.ts</code> - the TUI view</li><li><code>index.ts</code> - event wiring and the <code>/ctx</code> command</li></ul>',25)])])}const m=t(i,[["render",r]]);export{w as __pageData,m as default};
