import{_ as t,o,c as i,a0 as a}from"./chunks/framework.ChlVJx0I.js";const p=JSON.parse('{"title":"Edit assist corrects failing edit calls by mutating tool input, never writing files","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0020-edit-assist-corrects-by-mutating-tool-call-input.md","filePath":"adr/0020-edit-assist-corrects-by-mutating-tool-call-input.md","lastUpdated":1789783094000}'),s={name:"adr/0020-edit-assist-corrects-by-mutating-tool-call-input.md"};function r(n,e,l,c,d,h){return o(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="edit-assist-corrects-failing-edit-calls-by-mutating-tool-input-never-writing-files" tabindex="-1">Edit assist corrects failing edit calls by mutating tool input, never writing files <a class="header-anchor" href="#edit-assist-corrects-failing-edit-calls-by-mutating-tool-input-never-writing-files" aria-label="Permalink to &quot;Edit assist corrects failing edit calls by mutating tool input, never writing files&quot;">​</a></h1><p>The built-in edit tool fails about one call in seven in measured sessions (14.5 percent of 12,545 calls over 7.5 weeks; 12.7 percent in the last week of measurement). Three quarters of the failures are no-match: the model re-typed a multi-line oldText from memory with a small error, most often a leading-whitespace difference. The stock error message says nothing about where the text is or what differs, so the model spends two extra round trips recovering: a re-read or grep to learn the file&#39;s real text, then a retry.</p><p>Edit assist corrects this with two hooks around the built-in tool. Before execution, a <code>tool_call</code> hook reads the file and, for each edit that does not exact-match, runs an Extended match: pi&#39;s fuzzy normalization plus leading-whitespace-insensitive comparison. When the match is unique and the raw difference is a Whitespace-only diff, the hook rewrites that edit&#39;s oldText to the file&#39;s actual text in <code>event.input</code>, which pi documents as mutable in place. The built-in tool then executes the corrected input and exact-matches it by construction, so the session records a successful edit. After execution, a <code>tool_result</code> hook appends a bounded Diagnosis to the error of every call that still fails (nearest-region diff for no-match, occurrence list for ambiguous, one targeted hint for validation errors) and adds one honesty line to the success of a corrected call. pi skips the <code>tool_result</code> hook for validation failures: the call never executes, so no tool result event fires for it. The validation-error hint therefore rides on the <code>message_end</code> event of the toolResult message instead; every other Diagnosis, including the ambiguous occurrence list, rides on <code>tool_result</code> as described. The live TUI renders the tool result from <code>tool_execution_end</code>, which fires before <code>message_end</code>, so the validation-error hint first appears in the transcript on resume; the model context, the seam&#39;s target, receives it in the same turn.</p><p>The governing invariant: the extension never writes a file. Every write goes through the built-in tool, under pi&#39;s per-path file-mutation queue, so a correction can never race a parallel edit or write to the same path, and the transcript, the TUI diff, and the file bookkeeping all agree.</p><p>Rejected alternatives. Writing the file from inside the <code>tool_result</code> handler (report plus auto-apply): the handler runs outside the file-mutation queue, so a handler write can race a parallel call to the same path, and the transcript would show a failed tool call next to a changed file. Report-only: safe, but it leaves the retry round trip on the table; 61 percent of measured no-match failures fall in the safe Whitespace-only diff class, where the retry is pure waste. Extending the auto-fix to small character drift (for example <code>}),</code> versus <code>),</code>): the model composes its newText against the text it believed to exist, so a semantic difference risks replacing the wrong thing silently; those cases get a Diagnosis instead, and the model resends a corrected oldText. Stale-read detection (flagging a file that changed since the model&#39;s last read): measured incidence is 0.5 percent of no-match failures, not worth read-tracking state.</p><p>Not importing pi&#39;s internal matcher (<code>applyEditsToNormalizedContent</code> is not exported from the package): the extension needs only positive knowledge. A rewritten oldText is the file&#39;s real text, so the built-in exact-matches it; a wrong &quot;no match&quot; verdict is harmless because the hook leaves the input alone and pi&#39;s own exact and fuzzy paths still run.</p><p>Costs accepted. Every edit call, including the roughly 85 percent that would have succeeded, pays one extra file read and one match pass before execution; files over 300 KB or 20,000 lines skip the hook and run stock. The transcript of a corrected call shows the model&#39;s original arguments with a successful result; the honesty line in the success text is the bridge. The Diagnosis enters the model context and is resent on every later call until compaction, so every block is size-bounded.</p>',7)])])}const f=t(s,[["render",r]]);export{p as __pageData,f as default};
