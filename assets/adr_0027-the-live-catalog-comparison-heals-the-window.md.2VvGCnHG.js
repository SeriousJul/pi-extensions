import{_ as t,o,c as a,a0 as s}from"./chunks/framework.ChlVJx0I.js";const m=JSON.parse('{"title":"The live catalog comparison heals the window, not the fallback sentinel","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0027-the-live-catalog-comparison-heals-the-window.md","filePath":"adr/0027-the-live-catalog-comparison-heals-the-window.md","lastUpdated":1790371933000}'),n={name:"adr/0027-the-live-catalog-comparison-heals-the-window.md"};function i(r,e,d,c,h,l){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="the-live-catalog-comparison-heals-the-window-not-the-fallback-sentinel" tabindex="-1">The live catalog comparison heals the window, not the fallback sentinel <a class="header-anchor" href="#the-live-catalog-comparison-heals-the-window-not-the-fallback-sentinel" aria-label="Permalink to &quot;The live catalog comparison heals the window, not the fallback sentinel&quot;">​</a></h1><p>ADR 0019 gates its self-heal on one value: the check runs only when the session&#39;s model reports exactly the 128000 Fallback window. That gate is the reason a session died quietly. A llama.cpp service relaunched between two days carries a different <code>n_ctx</code> (the failing pair: 40192 from a launch on Sep 24, 160000 from the same preset on Sep 25), pi caches the derived window in its model store, and nothing revalidates that cache unless an extension forces a refresh. A session that selects the model from the cache holds a Cached window from a server launch that no longer exists. pi&#39;s <code>clampMaxTokensToContext</code> then computes <code>contextWindow - estimateContextTokens(context) - 4096</code>, floors it at <code>MIN_MAX_TOKENS = 1</code>, and sends <code>max_tokens: 1</code>. The model answered one thinking token, the turn recorded <code>stopReason: &quot;length&quot;</code>, and the next turn repeated it. The sentinel never matched, because 40192 is not 128000, so the heal bailed on every turn of that session. The Fallback window is one cause of a wrong window, not a test for it.</p><p>We compare instead of matching. At each of the two fixed Heal moments of a model selection, before its first request and after that request settles, llama refresh snapshots the registry&#39;s copy of the model, forces one catalog refresh scoped to the llama.cpp provider, reads the model back, and re-applies it when the value moved. The Live window arrives through pi&#39;s own provider, so this repo derives no <code>n_ctx</code> and mirrors no constant. <code>FALLBACK_WINDOW</code> leaves the module entirely; the term stays in the glossary as the name of the degenerate cached value. The compare applies a window in either direction: a server that comes back smaller shrinks the session, because a window we refuse to shrink is the same quiet lie that took an evening of journal reading to find.</p><p>Comparing the registry against itself is what makes this safe beside the context-cap extension. That extension caps at both layers: it re-registers each provider with <code>contextWindow: Math.min(cap, model.contextWindow)</code>, and it clamps the session&#39;s already-resolved model object in place. No extension-facing read returns a pre-cap window, so &quot;compare the raw catalog value with the live value&quot; has no raw operand. A before-and-after compare of the registry copy keeps both sides under the same cap, so a cap that hides drift is a cap doing its job, and a cap above a drifted window still shows the move.</p><p>One-shot budgeting cannot loop, and the reason is a pi detail worth writing down: <code>modelsAreEqual</code> in pi-ai compares only <code>id</code> and <code>provider</code>. The heal&#39;s own re-apply therefore emits no <code>model_select</code> for a same-model re-set, so a comparison-based trigger cannot re-arm the Attempt it just spent. A selection gets one Attempt per Heal moment, two in total, and a new selection re-arms both. A turn that ends <code>stopReason: &quot;length&quot;</code> with a near-empty output may spend an unspent Attempt on an extra compare, and can never re-arm one. A refresh that fails or times out spends nothing, exactly as before, so the second moment keeps the information the first one could not get.</p><p><strong>Considered options</strong>: read the live <code>meta.n_ctx</code> straight from the server for detection, using the model&#39;s own <code>baseUrl</code>, and force pi&#39;s refresh only when a drift shows - rejected because it duplicates the provider&#39;s derivation (<code>n_ctx ?? n_ctx_train ?? 128000</code>) and adds a second HTTP client to keep in sync, which is the fragility ADR 0019 already named once. Rejected with its cost: a forced refresh rewrites every llama model&#39;s cached window, so any model asleep at that instant gets 128000 written over its previous value; the post-request moment exists precisely to repair that case, so the cost is bounded by the mechanism we already trust. Sentinel plus an added trigger - rejected because it is the design that just failed, with a second trigger bolted on; every non-sentinel drift stays unhealed. A per-turn compare and a static window pin in the user&#39;s models config stay rejected for the reasons ADR 0019 gives.</p><p><strong>Consequences</strong>: up to two <code>model_change</code> entries per selection instead of one, since either moment can apply, and the transcript cost ADR 0019 accepted grows by the same amount. Every applied heal and every command run reports one line, <code>llama window: 40192 -&gt; 160000 (unsloth/qwen3.8-27b-dflash)</code>, because the failure this replaces was invisible until a journal was read by hand. A <code>/llama-window</code> command forces one compare, because pi emits no <code>model_select</code> when the same model is selected again, so the obvious human recovery from this bug spends and re-arms nothing; the doc comments that claim a re-select re-arms an Attempt are corrected with this change. Nothing goes upstream by deliberate choice, so the extension carries the whole burden of a stale cached window. Two related facts are recorded here but not solved by it: pi&#39;s own compaction threshold never fired on the failing session even though every reading of 0.87.0 says it should have, and <code>_omitRecoveryAttempt</code> writes a <code>context_edit</code> that invalidates the usage-backed compaction estimate, which makes a truncation suppress the compaction that would end it. The guard for a starved output budget, and the answer to that silence, are a separate module and a separate ADR.</p>',7)])])}const w=t(n,[["render",i]]);export{m as __pageData,w as default};
