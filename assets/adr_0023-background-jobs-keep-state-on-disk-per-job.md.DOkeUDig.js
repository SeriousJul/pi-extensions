import{_ as t,o,c as s,a0 as a}from"./chunks/framework.ChlVJx0I.js";const u=JSON.parse('{"title":"Background jobs keep state on disk, per job","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0023-background-jobs-keep-state-on-disk-per-job.md","filePath":"adr/0023-background-jobs-keep-state-on-disk-per-job.md","lastUpdated":1789926559000}'),n={name:"adr/0023-background-jobs-keep-state-on-disk-per-job.md"};function i(r,e,d,h,c,l){return o(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="background-jobs-keep-state-on-disk-per-job" tabindex="-1">Background jobs keep state on disk, per job <a class="header-anchor" href="#background-jobs-keep-state-on-disk-per-job" aria-label="Permalink to &quot;Background jobs keep state on disk, per job&quot;">​</a></h1><p>pi&#39;s bash tool is synchronous, so a command that takes minutes blocks the agent for its whole duration. The agents worked around it with nohup, sleep, and pgrep loops: 19 minutes of pure waiting, plus the context the loops cost, in the factory sessions of 2026-09-19 to 2026-09-20. The background jobs extension replaces the loops with three tools: <code>bash_bg</code> starts a command detached, <code>job_wait</code> blocks until it exits or a timeout, and <code>job_status</code> reports one job or lists all without waiting.</p><p>Three decisions shape the extension.</p><p><strong>Job state lives on disk, one directory per job, under pi&#39;s agent state root</strong> (the parent of the session directory). A job is a directory holding the manifest (command, cwd, label, pid, start time, log path), the command script, the log, the child pid, and the exit code. In-memory state would vanish when the session that started the job ends, and the whole point of backgrounding is that the work outlives the turn. On-disk state is what lets a new session find and wait on a job a previous session started, and it costs nothing: the directories are small, start removes directories older than seven days, and a machine-level bound of eight concurrent running jobs keeps the set finite.</p><p><strong>The exit code is recorded by a shell wrapper, not read from the spawned process.</strong> The wrapper runs the command script with its output to the log, records the child&#39;s pid, and writes the child&#39;s exit code to a file in the job directory on exit. Node&#39;s child process exit is only readable by the process that spawned it, so without the file a new session could see that the job is gone but never what it returned: a failed build and a successful one would look identical. The file makes the code knowable to any process, which is also what the cross-session wait story needs.</p><p><strong>The tools are agent tools, active by default.</strong> They are not in the /tools default-disabled list of user-only tools. The capability replaces a workaround the agent would otherwise improvise (the nohup-and-poll loops), so default-inactive would silently buy nothing; the prompt cost is three short descriptions and three guidelines. The prompt guideline carries the when-to-use rule: background a command only when it is expected to take more than about ten seconds, or when other work can proceed while it runs. Plain bash stays the default.</p><p>Rejected alternatives: a single combined tool (start and wait in one call) would not save time - the whole point is that the agent works while the command runs, which needs the start and the wait in separate turns. A kill tool is out of scope for the first version: the agent signals the job through the ordinary bash tool with the pid the status reports, and a dedicated kill is a candidate if sessions show the need. Per-user or per-project job namespaces are rejected in favor of the machine-local root: the bound is machine-level by design, and the operator runs several agents on one machine, not in separate accounts. Windows is out of scope; the wrapper is POSIX and the extension set targets the operator&#39;s Linux and macOS machines.</p>',7)])])}const b=t(n,[["render",i]]);export{u as __pageData,b as default};
