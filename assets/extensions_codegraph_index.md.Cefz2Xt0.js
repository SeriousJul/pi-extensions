import{_ as t,o as s,c as a,a0 as o}from"./chunks/framework.ChlVJx0I.js";const i="/pi-extensions/assets/codegraph-status.D847qm7S.png",u=JSON.parse('{"title":"codegraph extension","description":"","frontmatter":{},"headers":[],"relativePath":"extensions/codegraph/index.md","filePath":"extensions/codegraph/index.md","lastUpdated":1789775493000}'),n={name:"extensions/codegraph/index.md"};function r(d,e,l,h,c,p){return s(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="codegraph-extension" tabindex="-1">codegraph extension <a class="header-anchor" href="#codegraph-extension" aria-label="Permalink to &quot;codegraph extension&quot;">​</a></h1><p>The codegraph extension embeds the <a href="https://www.npmjs.com/package/@colbymchenry/codegraph" target="_blank" rel="noreferrer"><code>@colbymchenry/codegraph</code></a> library in-process (no MCP server, no daemon). It gives the agent transparent semantic code search over the current project and over dependency sources. Each git worktree gets its own index, so results always reflect the branch being edited.</p><p><a href="/pi-extensions/extensions/codegraph/internals.html">Internals</a>: the runtime patch, the full index lifecycle, root resolution, labels, and the module map.</p><h2 id="screenshot" tabindex="-1">Screenshot <a class="header-anchor" href="#screenshot" aria-label="Permalink to &quot;Screenshot&quot;">​</a></h2><p>The bare <code>/codegraph</code> command over a small fixture repo: the root with its index counts, index state, last reconcile, watcher, auto-index, and per-tool usage.</p><p><img src="'+i+'" alt="codegraph status"></p><h2 id="tools" tabindex="-1">Tools <a class="header-anchor" href="#tools" aria-label="Permalink to &quot;Tools&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Tool</th><th>Purpose</th></tr></thead><tbody><tr><td><code>codegraph_search</code></td><td>Quick symbol search by name. Locations only, no code.</td></tr><tr><td><code>codegraph_impact</code></td><td>Show what could break if a symbol changes, by depth. The prompt note tells the agent to call it before a refactor of a symbol.</td></tr><tr><td><code>codegraph_node</code></td><td>Read a file (line numbers, dependents header) or a symbol (signature, body, top callers and callees). File mode behaves like the built-in <code>read</code>, plus which files depend on it.</td></tr><tr><td><code>codegraph_explore</code></td><td>Source, call paths, and relationships for an area in one call. The default first tool for &quot;how does X work&quot; questions. The call trail carries the caller and callee information the dedicated tools used to give.</td></tr></tbody></table><p>The caller and callee tools (<code>codegraph_callers</code>, <code>codegraph_callees</code>) keep their definitions in the handlers module but are not registered (issue #72): <code>codegraph_explore</code> carries their information in its call trail, and the measurement showed the agent answered those jobs with explore. Re-adding either is one line in the <code>TOOL</code> map, a registration change, not a re-implementation.</p><p>Every tool takes an optional <code>projectRoot</code> argument. Without it, the call is served from the worktree the call was made in, resolved automatically. <code>codegraph_search</code> also takes <code>kind</code> (a single kind string or an array).</p><h2 id="per-worktree-indexes" tabindex="-1">Per-worktree indexes <a class="header-anchor" href="#per-worktree-indexes" aria-label="Permalink to &quot;Per-worktree indexes&quot;">​</a></h2><p>Every git worktree gets its own index under <code>&lt;worktree&gt;/.codegraph/</code>:</p><ul><li><strong>Borrowed indexes are never served.</strong> If the nearest initialized index lives in another worktree, it is not used; the worktree gets its own index instead.</li><li><strong>Seeding.</strong> A new worktree copies the index database from a sibling worktree of the same repository (shared common git dir) and reconciles, instead of building from scratch.</li><li><strong>Prewarm.</strong> The first agent turn of a session starts a background build for a worktree that has no index. The first <code>codegraph_*</code> call waits for that build and is served from it, so the build is never paid twice. A background failure never becomes a tool result.</li><li><strong>Freshness.</strong> The index is reconciled on first use per session, then the file watcher keeps it current. If watching is disabled, the index is reconciled before every query.</li><li><strong>Staleness gate at emission.</strong> Sliced source is checked against its indexed record before it is emitted. A drifted file never emits a slice; a small one serves its full current source instead.</li></ul><p>The full lifecycle (prewarm internals, watcher degradation, cross-process build locks, the staleness gate) is on the <a href="/pi-extensions/extensions/codegraph/internals.html">internals</a> page.</p><h2 id="querying-dependency-sources-projectroot" tabindex="-1">Querying dependency sources (<code>projectRoot</code>) <a class="header-anchor" href="#querying-dependency-sources-projectroot" aria-label="Permalink to &quot;Querying dependency sources (`projectRoot`)&quot;">​</a></h2><p>A <code>projectRoot</code> argument serves the call from a <strong>dependency source</strong>: the source tree of one version of one dependency, typically in the opensrc cache home. The argument takes the path rule (absolute, <code>~</code>, or relative to the working directory). Rules:</p><ul><li><strong>Trusted roots gate the build.</strong> An index for a named root is built only at or under a trusted root: the opensrc cache home (<code>OPENSRC_HOME</code>, else <code>~/.opensrc</code>, when it exists), the <code>CODEGRAPH_PI_TRUSTED_ROOTS</code> entries, and the roots <code>/codegraph add</code> stores for the session. A build outside every trusted root is refused with a reason that names the command to run. An index that already exists is served wherever it lives.</li><li><strong>Labels.</strong> Every result from a named root is prefaced with one line: <code>Project: &lt;name&gt; @&lt;version&gt; - &lt;absolute path&gt;</code>. The label comes from the opensrc cache manifest. It makes a wrong version visible and gives the agent the path to hand to its next read or grep.</li><li><strong>Snapping.</strong> A directory inside a cache entry serves that entry&#39;s tree, so a sub-directory of a dependency works.</li><li><strong>On demand, always finished.</strong> A named root is built on demand, and a call that starts the build waits for it and always finishes: no timeout, no &quot;retry later&quot;. While the build runs, its progress (the file counts the adapter reports) streams to the caller as tool updates, so the wait is visible and bounded instead of a silent hang. Later calls pay only the reconcile. The root is reconciled before every query, cached per root for the session, and never prewarmed, seeded, or watched.</li><li><strong>Missing sources.</strong> A named directory that does not exist fails with <code>no such directory (&lt;abs&gt;)</code> plus a hint that names the <code>opensrc fetch</code> for the dependency. The extension never fetches on the agent&#39;s behalf.</li></ul><h3 id="real-example-with-opensrc" tabindex="-1">Real example with opensrc <a class="header-anchor" href="#real-example-with-opensrc" aria-label="Permalink to &quot;Real example with opensrc&quot;">​</a></h3><p>Say the agent edits code that calls into the <code>herdr</code> library, and its source is in the opensrc cache. The agent finds the cache directory and queries it:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> opensrc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> herdr</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">/home/seriousjul/.opensrc/repos/github.com/ogulcancelik/herdr/0.0.0</span></span></code></pre></div><p>Then it passes that directory as <code>projectRoot</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>codegraph_explore(</span></span>\n<span class="line"><span>  query: &quot;worktree create&quot;,</span></span>\n<span class="line"><span>  projectRoot: &quot;/home/seriousjul/.opensrc/repos/github.com/ogulcancelik/herdr/0.0.0&quot;</span></span>\n<span class="line"><span>)</span></span></code></pre></div><p>The first call builds the index for that cache entry (a full build, since named roots are not prewarmed); later calls in the session hit the cache. Every result is labeled, so the agent never confuses herdr symbols with symbols of the project it edits:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Project: herdr @0.0.0 - /home/seriousjul/.opensrc/repos/github.com/ogulcancelik/herdr/0.0.0</span></span>\n<span class="line"><span>...symbols, call paths, and source slices from the dependency...</span></span></code></pre></div><p>One <code>projectRoot</code> serves one call. To read the dependency&#39;s text that is not a symbol (comments, config, log strings), the agent greps the same path:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rg</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;pattern&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensrc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> herdr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="recommended-global-agents-md-section" tabindex="-1">Recommended global <code>AGENTS.md</code> section <a class="header-anchor" href="#recommended-global-agents-md-section" aria-label="Permalink to &quot;Recommended global `AGENTS.md` section&quot;">​</a></h2><p>The system prompt note (below) teaches the agent about codegraph inside each session. For dependency sources, add this section to your global <code>~/AGENTS.md</code> so every agent of every project learns the pattern, including the grep form the prompt note does not cover:</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## Source Code Reference</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Source code for dependencies is cached at </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">`~/.opensrc/`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">. A dependency is</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">indexed on first use: the first codegraph call to its cache directory builds</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">the index inline, may wait a few seconds, and reports progress as it builds.</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Later calls pay only the reconcile. To explore a dependency, use the</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">codegraph tools with </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">`projectRoot`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> set to its cache directory</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">`opensrc path &lt;package&gt;`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">). Use </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">`codegraph_explore`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> for symbol source and</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">call paths, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">`codegraph_search`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> for symbol names, and </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">`codegraph_node`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> to</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">read a file. Use grep for text that is not a symbol (comments, config</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">values, log strings):</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">```bash</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rg</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;pattern&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensrc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> path</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">packag</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">```</span></span></code></pre></div><p>The block only pays off when the opensrc cache exists, because the prompt note&#39;s dependency line appears only when at least one trusted root exists on disk.</p><h2 id="system-prompt-note" tabindex="-1">System prompt note <a class="header-anchor" href="#system-prompt-note" aria-label="Permalink to &quot;System prompt note&quot;">​</a></h2><p>Every agent turn appends one note to the system prompt: a first line that states the index state (ready, building, or none), then fixed policy lines that say which tool fits which job. It is the only codegraph steering text in the prompt, so it is the single place to change policy. The impact line targets the trigger the agent actually meets (a refactor of a symbol, not every change), and the dependency-source line carries the first-use promise (the first call to a dependency builds its index and may wait, and the build reports progress). A dependency-source line joins the block only when at least one trusted root exists on disk. The note carries no per-dependency state, so the system prompt stays stable turn to turn and the provider cache stays warm. The note appears only when at least one <code>codegraph_*</code> tool is active, a call can actually be served from the working directory, and the runtime stack passes its preflight check.</p><h2 id="codegraph-command" tabindex="-1"><code>/codegraph</code> command <a class="header-anchor" href="#codegraph-command" aria-label="Permalink to &quot;`/codegraph` command&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Verb</th><th>Effect</th></tr></thead><tbody><tr><td><code>/codegraph</code></td><td>Index status for the current directory, the named roots this session opened, and the trusted roots with their origin.</td></tr><tr><td><code>/codegraph status [path]</code></td><td>Index status of the named root the path names, or the session root without a path.</td></tr><tr><td><code>/codegraph init [path]</code></td><td>Force a full rebuild of the index for the given root (trust-gated), or the session root.</td></tr><tr><td><code>/codegraph seed [path]</code></td><td>Re-seed the session index from a sibling worktree, or seed the named root a path names. Then reconcile.</td></tr><tr><td><code>/codegraph uninit [path]</code></td><td>Remove the index for the given root (or the session root), with its usage log. Asks for confirmation.</td></tr><tr><td><code>/codegraph add &lt;path&gt;</code></td><td>Trust a directory for named-root builds for the rest of the session.</td></tr><tr><td><code>/codegraph auto on|off</code></td><td>Toggle automatic index creation for the session.</td></tr></tbody></table><p>Status shows local usage from <code>&lt;worktree&gt;/.codegraph/usage.jsonl</code>: per-tool counts, successful and failed calls, time since the last call, and the reason of the newest failure. The log is append-only, never leaves the machine, and is removed with the index by <code>/codegraph uninit</code>. Delete the file to start the counts over.</p><h2 id="environment-variables" tabindex="-1">Environment variables <a class="header-anchor" href="#environment-variables" aria-label="Permalink to &quot;Environment variables&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Variable</th><th>Effect</th></tr></thead><tbody><tr><td><code>CODEGRAPH_PI_AUTO_INDEX</code></td><td><code>0</code>/<code>false</code>/<code>off</code> disables automatic index creation (tools return the fallback line until an index exists).</td></tr><tr><td><code>CODEGRAPH_PI_PREWARM</code></td><td><code>0</code> disables the first-turn background build. Existing indexes are never prewarmed.</td></tr><tr><td><code>CODEGRAPH_PI_SEEDING</code></td><td><code>0</code>/<code>false</code>/<code>off</code> disables seeding from sibling worktrees (first build is from scratch).</td></tr><tr><td><code>CODEGRAPH_NO_WATCH</code></td><td><code>1</code> disables the file watcher (reconcile before every query instead).</td></tr><tr><td><code>CODEGRAPH_PI_SQLITE_SHIM</code></td><td>Set to <code>bun</code> to force the shim&#39;s <code>bun:sqlite</code> path (used by the bun-only test).</td></tr><tr><td><code>CODEGRAPH_PI_TRUSTED_ROOTS</code></td><td>PATH-style list of trusted roots for named-root builds. Entries that do not exist are ignored.</td></tr><tr><td><code>OPENSRC_HOME</code></td><td>The dependency cache home. When it exists it is a trusted root and the source of project labels and fetch hints. When set but missing, the extension behaves as if opensrc were absent.</td></tr><tr><td><code>CODEGRAPH_NO_FAST_INIT</code></td><td>Set to <code>1</code> automatically on the bun runtime unless the user set it (bun&#39;s SQLite engine rejects the fast-init journal-mode change).</td></tr><tr><td><code>CODEGRAPH_TELEMETRY</code></td><td>Set to <code>0</code> by this extension unless already set.</td></tr><tr><td><code>CODEGRAPH_NO_UPDATE_CHECK</code></td><td>Set to <code>1</code> by this extension unless already set.</td></tr></tbody></table><h2 id="failure-contract" tabindex="-1">Failure contract <a class="header-anchor" href="#failure-contract" aria-label="Permalink to &quot;Failure contract&quot;">​</a></h2><p>Every tool failure ends with the same line, which tells the model to fall back to the built-in tools:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>codegraph is unavailable (&lt;reason&gt;). Use the built-in read and grep tools instead.</span></span></code></pre></div><p>The reason is recorded in the worktree&#39;s usage log, so <code>/codegraph status</code> shows it after the tool result has scrolled away. The extension never turns a codegraph failure into a session error.</p>',41)])])}const k=t(n,[["render",r]]);export{u as __pageData,k as default};
