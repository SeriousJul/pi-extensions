import{_ as o,o as t,c as s,a0 as a}from"./chunks/framework.ChlVJx0I.js";const u=JSON.parse('{"title":"Named project roots for dependency sources","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0003-named-project-roots-for-dependency-sources.md","filePath":"adr/0003-named-project-roots-for-dependency-sources.md","lastUpdated":1789271221000}'),n={name:"adr/0003-named-project-roots-for-dependency-sources.md"};function r(d,e,i,c,l,h){return t(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="named-project-roots-for-dependency-sources" tabindex="-1">Named project roots for dependency sources <a class="header-anchor" href="#named-project-roots-for-dependency-sources" aria-label="Permalink to &quot;Named project roots for dependency sources&quot;">​</a></h1><p>The agent reads dependency source through the opensrc cache, outside the session root. Before this decision only <code>codegraph_node</code> file mode could reach it, by accident: an absolute path outside the session root built an index in a tree nobody asked about, served it with no label, and <code>codegraph_explore</code> answered the same path from the wrong project. We decided that all six tools take an optional <code>projectRoot</code>, that a call naming one is served from it and labeled with its name and version, that building there is allowed only under a Trusted root, and that serving an index which already exists is allowed anywhere.</p><h2 id="the-decision" tabindex="-1">The decision <a class="header-anchor" href="#the-decision" aria-label="Permalink to &quot;The decision&quot;">​</a></h2><ul><li>All six tools take an optional <code>projectRoot</code>. It is the call&#39;s anchor: file arguments resolve inside it and never move it.</li><li>Building an index in a Named root is allowed only under a Trusted root: <code>$OPENSRC_HOME</code> or <code>~/.opensrc</code>, plus <code>CODEGRAPH_PI_TRUSTED_ROOTS</code>, plus <code>/codegraph add &lt;path&gt;</code> for the session. Serving an existing index is not gated: the act that needs consent is the one with a cost and a side effect.</li><li>One root serves one call. No call spans two roots, because the graph is per-root and edges exist only inside one index.</li><li>A Named path inside a cache entry snaps up to that entry, so one fetched source has one complete index instead of one partial index per member crate.</li><li>A Dependency source gets no watcher and no prewarm. It reconciles before each query, which measured about 22 ms per 1 000 files.</li><li>A version mismatch stays visible instead of being detected. opensrc&#39;s cache can hold a version the project does not use; the label is what lets the agent see it.</li></ul><h2 id="considered-options" tabindex="-1">Considered Options <a class="header-anchor" href="#considered-options" aria-label="Permalink to &quot;Considered Options&quot;">​</a></h2><ul><li><strong>A name parameter resolved through opensrc.</strong> Rejected on evidence. <code>opensrc path &lt;name&gt;</code> resolves against a registry, not against the cache: <code>opensrc path crates:accesskit</code> fetched 0.25.0 over the network and rewrote the cache manifest while 0.24.1 sat cached, and <code>--cwd</code> did not switch the registry for a Rust project (<code>Package &quot;accesskit&quot; not found on npm</code>). Its lockfile detection is npm-only, and otherwise it uses the latest published version. A name therefore cannot be resolved offline to the version a project pins, and resolving it inside a tool would put a network fetch in a search call. The agent already holds the absolute path: <code>opensrc path</code> runs in bash, in the open.</li><li><strong>Anchor inference</strong> - extend <code>codegraph_node</code>&#39;s file anchor to all six tools, so any path-shaped argument moves the root. Rejected. It reverses the rule that a <code>file</code> used to disambiguate a symbol never moves the anchor, and <code>codegraph_explore</code>&#39;s query is free text, so inference there is guesswork.</li><li><strong>A central index cache outside the tree.</strong> Not possible. <code>CODEGRAPH_DIR</code> renames the index directory but it must stay a single path segment inside the root; a separator, <code>..</code>, or an absolute path is ignored. A symlink re-opens the lockfile and inode-replacement problems ADR 0002 rejected, and copying the source would make every emitted path a lie. The index lives in-tree, so <code>opensrc remove</code> and <code>opensrc clean</code> remove it with the source - the right lifetime for an index that is disposable and rebuildable in seconds.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>This reverses &quot;there is no <code>projectPath</code> parameter on any tool&quot; in the extension README and the assertion in <code>tests/codegraph/tools.test.ts</code>. Both are rewritten, not deleted: the parameter is bounded, and the bound is the point. Upstream&#39;s word stays out of the schema, because upstream&#39;s parameter is unbounded.</li><li>Results from a Named root must be labeled, since two roots both contain <code>src/lib.rs</code>. The label comes from <code>opensrc list --json</code>: longest path prefix wins, all names on one path are shown, and a root that matches no entry falls back to a path form. A missing label never fails a call. One module names opensrc, the way <code>indexAdapter.ts</code> names the codegraph library.</li><li>Measured cost of a build: 1.3 s and 34.6 MB for 390 files (egui 0.36.1), 2.4 s and 99 MB for 1 218 files (pi 0.84.4). Disk is 3-5x the source, which is why the Trusted-root bound exists and why no prewarm was added.</li><li>The prompt note gains one policy line, and only when a Trusted root exists on disk. Where no call can build, the note must not advertise one.</li></ul>',8)])])}const m=o(n,[["render",r]]);export{u as __pageData,m as default};
