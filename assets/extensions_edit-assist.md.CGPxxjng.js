import{_ as t,o as i,c as s,a0 as n}from"./chunks/framework.ChlVJx0I.js";const u=JSON.parse('{"title":"edit-assist extension","description":"","frontmatter":{},"headers":[],"relativePath":"extensions/edit-assist.md","filePath":"extensions/edit-assist.md","lastUpdated":1789791105000}'),a={name:"extensions/edit-assist.md"};function o(l,e,d,r,h,c){return i(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="edit-assist-extension" tabindex="-1">edit-assist extension <a class="header-anchor" href="#edit-assist-extension" aria-label="Permalink to &quot;edit-assist extension&quot;">​</a></h1><p>Corrects and diagnoses failing edit tool calls around the built-in edit tool (<a href="/pi-extensions/adr/0020-edit-assist-corrects-by-mutating-tool-call-input.html">ADR 0020</a>). The built-in edit fails about one call in seven in measured sessions, most often on a leading-whitespace difference in a re-typed oldText. The extension fixes the safe cases before execution and diagnoses the rest after execution. Every output is size-bounded: a Diagnosis re-enters the model context on every later call until compaction. The extension never writes a file itself; every write goes through the built-in tool. The stock error text is always kept; a Diagnosis is appended after it, never replaces it.</p><h2 id="behavior" tabindex="-1">Behavior <a class="header-anchor" href="#behavior" aria-label="Permalink to &quot;Behavior&quot;">​</a></h2><ul><li><strong>Input correction.</strong> A <code>tool_call</code> hook reads the target file before the built-in edit runs. For each edit whose oldText does not exact-match, it runs the Extended match: pi&#39;s fuzzy normalization plus a leading-whitespace-insensitive comparison. When the match is unique and the raw difference is a Whitespace-only diff, the hook replaces that edit&#39;s oldText with the file&#39;s actual text in <code>event.input</code>. The built-in tool then executes the corrected input, and the success result gains one honesty line naming the corrected line, so the transcript (which shows the model&#39;s original arguments) and the file agree. An edit with zero or several Extended matches, or with any character drift, is left alone and the built-in&#39;s own exact and fuzzy paths still run on it.</li><li><strong>No-match Diagnosis.</strong> A <code>tool_result</code> hook scoped to the edit tool reacts to the stock no-match errors (<code>Could not find the exact text in ...</code> and <code>Could not find edits[i] in ...</code>). One block per edit the built-in could not find with its exact or fuzzy step - including an edit that reaches only the Extended match, which the built-in does not apply, so a leading-whitespace drift that was never corrected (several Extended matches) still fails and still gets a Diagnosis. Each block names the Nearest region line range (a trailing newline does not start a new line, so the range never names a line past the end of the file), states a Whitespace-only difference explicitly when the only difference is leading whitespace on the same line count, and carries the unified diff between the oldText and the region&#39;s real text. When no part of the file resembles the oldText, the block says <code>No candidate region</code>.</li><li><strong>Ambiguous Diagnosis.</strong> When the oldText matched the file in several places, the stock error is kept and the occurrence line numbers are appended, max 10, each with one context line. The extension re-derives the occurrence set and gives up (stock error only) when the file no longer reproduces the stock count.</li><li><strong>Malformed-argument hint.</strong> A call that fails argument validation never executes, so pi fires no <code>tool_result</code> hook for it. The hint rides on the <code>message_end</code> event of the toolResult message instead: a read-tool-shaped call (path with offset and limit, no edits) is told to use the read tool, and an edits value sent as a string gets a shape hint. Any other malformed shape is left unchanged.</li><li><strong>Size bounds.</strong> One no-match diff is capped at 40 lines and 1200 characters; a longer diff is cut and marked <code>... (diff truncated)</code>. Diff inputs longer than 300 lines per side are head-capped before the diff runs. A file window below a 0.5 line-similarity score is not a candidate region.</li><li><strong>Large-file guard.</strong> Files over 300 KB or 20,000 lines run stock: the built-in&#39;s behavior stands alone. A file exactly at either limit still gets the Diagnosis; the trailing newline of a normal file does not count as a line.</li><li><strong>Off switch.</strong> The extension is on by default; disabled, the built-in edit tool runs stock: no correction, no honesty note, no Diagnosis, no hint.</li></ul><h2 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to &quot;Settings&quot;">​</a></h2><p>The <code>edit-assist</code> section of <code>settings.json</code>. The project <code>&lt;cwd&gt;/.pi/settings.json</code> overrides the global settings file, key by key.</p><table tabindex="0"><thead><tr><th>Key</th><th>Type</th><th>Default</th><th>Meaning</th></tr></thead><tbody><tr><td><code>enabled</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Enable the whole extension; disabled runs the built-in stock.</td></tr></tbody></table><p>Example:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;edit-assist&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;enabled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>A malformed value falls back to its default and is reported with one startup notification per error.</p><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h2><ul><li><strong>Seam.</strong> The decision logic lives in <code>core.ts</code>, which is engine-free (matching, region search, diffing, formatting) and fully unit-tested against fixtures extracted from real pi sessions. <code>settings.ts</code> reads the settings section. <code>index.ts</code> is the thin pi wiring around the <code>tool_call</code>, <code>tool_result</code>, and <code>message_end</code> hooks.</li><li><strong>Out of scope.</strong> Extending the input correction beyond the Whitespace-only diff class: the model composes its newText against the text it believed to exist, so a character drift risks replacing the wrong thing silently; those cases get a Diagnosis instead, and the model resends a corrected oldText.</li></ul>`,12)])])}const g=t(a,[["render",o]]);export{u as __pageData,g as default};
